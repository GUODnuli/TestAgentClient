# 全局默认配置

[default]
# 工作目录
workdir = "."

# 日志级别：DEBUG < INFO < WARNING < ERROR
# 注意：DEBUG 级别会输出详细的调试信息，INFO 级别输出一般运行信息
log_level = "INFO"

# 日志文件路径
log_file = "./logs/app.log"

# 数据库路径
database_path = "./storage/tasks.db"
